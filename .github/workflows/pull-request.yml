name: Merge pull request

on:
  pull_request_review:
    types: [submitted]


jobs:
  # labeled:
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: check context
  #     uses: actions/github-script@v6
  #     with: 
  #       github-token: ${{secrets.PAT_TOKEN}}
  #       script: |
  #         console.log('labels', context.pull_request.labels);
  #         console.log('author_association', context.review.author_association);
  approved:
    if: github.event.review.state == 'approved'
    runs-on: ubuntu-latest
    steps:
    - name: check context
      uses: actions/github-script@v6
      with: 
        script: |
          console.log('labels', context.payload.pull_request.labels);
          console.log('author_association', context.payload.review.author_association);
